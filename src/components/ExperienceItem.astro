---
const {date, title, description, link} = Astro.props
---
<li class="mb-10 ms-4">
        <div class="absolute w-3 h-3 bg-gray-200 rounded-full mt-1.5 -start-1.5 border border-gray-100 dark:border-gray-900 dark:bg-gray-700"></div>
        <time class="mb-3 text-sm font-normal leading-none text-gray-400 dark:text-gray-500">{date}</time>
        <h3 class="pt-3 text-lg font-semibold text-rose-600">{title}</h3>
        <p class="mb-4 text-base font-normal text-gray-500 dark:text-gray-400 text-pretty">{description}</p>
        { link && (
        <a href={link} role="link" class="inline-flex items-center text-lg font-medium text-rose-500 dark:text-teal-200 dark:hover:text-rose-300 hover:text-rose-700">
          know more{" "}
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="w-5 icon icon-tabler icon-tabler-chevron-right"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            stroke-width="2"
            stroke="currentColor"
            fill="none"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <>
              <path stroke="none" d="M0 0h24v24H0z" fill="none" />
              <path d="M9 6l6 6l-6 6" />
            </>
          </svg>
        </LinkInline>
      )}
    <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 5h12m0 0L9 1m4 4L9 9"/>
  </svg></a>
    </li>